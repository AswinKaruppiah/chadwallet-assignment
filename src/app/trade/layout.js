export const metadata = {
  title: "Trade Memecoins Instantly | ChadWallet Dashboard",
  description: "Swap tokens, trace live trades, and track top holders in real-time with the ChadWallet terminal.",
  alternates: {
    canonical: "https://chadwallet-assignment.vercel.app/trade",
  },
};

export default function TradeLayout({ children }) {
  return <>{children}</>;
}
